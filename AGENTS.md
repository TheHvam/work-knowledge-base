# Repository Workflow

After changing this knowledge base, verify the affected files and immediately create a Git commit containing only the files changed for the task. Use a concise imperative commit message.

Do not include unrelated working-tree changes. Push to GitHub only when the user explicitly requests it.
